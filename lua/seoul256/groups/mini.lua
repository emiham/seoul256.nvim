local colors = require("seoul256.colors")

return {
  MiniIndentscopeSymbol = colors.line_numbers_fg,

  MiniPickBorder = colors.border,
  MiniPickBorderBusy = colors.red,
  MiniPickBorderText = colors.red,
  MiniPickCursor = colors.red,
  -- MiniPickIconDirectory = colors.green,
  -- MiniPickNormal = colors.fg,
  -- MiniPickIconFile = colors.red,
  -- MiniPickHeader = colors.red,
  MiniPickMatchCurrent = colors.red,
  MiniPickMatchMarked = colors.red,
  MiniPickMatchRanges = colors.red,
  MiniPickPreviewLine = colors.red,
  MiniPickPreviewRegion = colors.red,
  MiniPickPrompt = colors.dark_fg,
}
